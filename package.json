{
  "name": "ssechat",
  "version": "1.0",
  "description": "Chat Application",
  "main": "backend/server.js",
  "author": "SSE World",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "server": "nodemon backend/server.js",
    "build": "yarn install --legacy-peer-deps && yarn install --legacy-peer-deps --prefix frontend && yarn run build --prefix frontend"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.1",
    "nodemon": "^3.0.1",
    "socket.io": "^4.7.2"
  }
}
